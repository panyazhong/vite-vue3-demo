const locale = {
  state: {
    language: 'zh',
  },
  mutations: {
    SET_LANGUAGE: (state: any, language: string) => {
      state.language = language;
    },
  },
  actions: {},
};

export default locale;
